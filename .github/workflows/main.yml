name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Make slides
      run: jupyter nbconvert Decorators_Dataclass_IDE.ipynb --to slides --post serve --SlidesExporter.reveal_scroll=True

